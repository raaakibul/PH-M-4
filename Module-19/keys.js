const computer ={
    brand:'lenovo',
    price:35000,
    procesor: 'intel',
    hdd:'512gb',
    monitor: 'hp'
}

const keys = Object.keys(computer)
console.log(keys);

const values = Object.values(computer)
console.log(values);